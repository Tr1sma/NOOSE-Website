@* Druck-Sektion „Verknüpfte Akten" (gemeinsam für alle Druckansichten; rendert nichts bei leer). *@
@if (Entries.Count > 0)
{
    <section>
        <h2>@Heading</h2>
        <table class="druck-tabelle">
            <thead>
                <tr><th>Typ</th><th>Akte</th><th>Bezeichnung</th></tr>
            </thead>
            <tbody>
                @foreach (var v in Entries)
                {
                    <tr>
                        <td>@v.OtherType</td>
                        <td>@v.OtherDesignation</td>
                        <td>@(string.IsNullOrWhiteSpace(v.Label) ? "—" : v.Label)</td>
                    </tr>
                }
            </tbody>
        </table>
    </section>
}

@code {
    [Parameter, EditorRequired] public List<LinkDisplay> Entries { get; set; } = new();
    [Parameter] public string Heading { get; set; } = "Verknüpfte Akten";
}
