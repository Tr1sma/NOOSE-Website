@* Legacy doc-template routes redirect to the bundled template management. *@
@page "/admin/dok-vorlagen"
@page "/dok-vorlagen"
@attribute [Authorize(Policy = Policies.LeadershipPage)]
@inject NavigationManager Nav

<PageTitle>Vorlagen · NOOSE</PageTitle>

@code {
    protected override void OnInitialized()
        => Nav.NavigateTo("/admin/vorlagen?tab=dok-vorlagen", replace: true);
}
